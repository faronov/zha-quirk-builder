name: Build desktop applications

on:
  pull_request:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.artifact }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact: windows-x64
          - os: macos-13
            artifact: macos-x64
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install project
        run: python -m pip install --disable-pip-version-check -e ".[dev,build]"

      - name: Test
        run: |
          python -m ruff check src tests
          python -m pytest -q

      - name: Build application
        run: python -m PyInstaller --noconfirm --clean packaging/zha-quirk-builder.spec

      - name: Smoke test Windows executable
        if: runner.os == 'Windows'
        shell: pwsh
        run: '& "dist/ZHA Quirk Builder.exe" --self-test; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }'

      - name: Smoke test macOS application
        if: runner.os == 'macOS'
        run: '"dist/ZHA Quirk Builder.app/Contents/MacOS/ZHA Quirk Builder" --self-test'

      - name: Package Windows application
        if: runner.os == 'Windows'
        shell: pwsh
        run: Compress-Archive -Path "dist/ZHA Quirk Builder.exe" -DestinationPath "ZHA-Quirk-Builder-windows-x64.zip"

      - name: Package macOS application
        if: runner.os == 'macOS'
        run: ditto -c -k --sequesterRsrc --keepParent "dist/ZHA Quirk Builder.app" "ZHA-Quirk-Builder-macos-x64.zip"

      - uses: actions/upload-artifact@v4
        with:
          name: ZHA-Quirk-Builder-${{ matrix.artifact }}
          path: ZHA-Quirk-Builder-*.zip
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: release
          merge-multiple: true
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" release/*.zip --generate-notes --verify-tag
